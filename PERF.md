# PERF.md — 성능 병목 분석과 before/after 수치

> 측정은 벽시계가 아니라 `MarketFeed.pump()` 로 **결정론적**으로 배치를 밀어넣는
> `integration_test` 시나리오로 수행한다. 기본 seed(20260703)이므로 tick 수열이
> 항상 동일 → 후보(baseline/after) 간 공정 비교가 가능하다.
>
> **단일 측정은 신뢰하지 않는다.** 아래 수치는 **동일 조건 5회 반복의 중앙값(median)**
> 이다. 실기기는 발열·백그라운드로 run-to-run 편차가 커서(예: baseline 예산 초과
> 프레임이 44~57 사이로 출렁) 단일 run의 ±1~2 차이는 노이즈다.

## 측정 환경

- 기기: **EF550R (Android 11, arm64)** — 실기기, **profile 모드**
- 렌더러: Impeller (Vulkan) / 프레임 예산: **16.67ms (60fps)**
- **예산을 다투는 주체**: Dart 코드는 **UI task runner의 root isolate 하나**에서만 실행된다.
  tick 흡수(`_onBatch`)·`_flush()`·build·layout이 전부 같은 16.67ms를 나눠 쓴다.
  반면 **raster는 별도 스레드**라 빌드 예산과 경쟁하지 않고 파이프라인으로 겹친다.
  → 아래에서 래스터 +18%를 "병목 아님"으로 판정하는 근거가 이 스레드 분리다.
  ("Flutter는 single thread"는 부정확하다. 엔진은 Platform/UI/Raster/IO task runner를
  쓰고, **단일한 것은 Dart가 도는 UI 아이솔레이트**다.)
- 도구: `IntegrationTestWidgetsFlutterBinding.watchPerformance` → `TimelineSummary`
- 원본 데이터
  - `perf/runs/run_1..5.json` — 1차(2026-07-28, 랭킹=`SplayTreeSet`)
  - `perf/runs/topk_run_1..5.json` — 2차(2026-08-06, 랭킹=선형 선택)
  - `perf/paired_result.json`, `perf/baseline_result.json` — 초기 단발 측정
- 집계: `python3 perf/summarize.py` (5회 중앙값 표를 생성. 손계산 금지)
- 랭킹 자료구조 단독 비교: `dart run perf/top20_bench.dart` (§5.2)

## 재현 방법

```bash
# 1회
flutter drive --no-dds \
  --driver=test_driver/integration_test.dart \
  --target=integration_test/perf_test.dart \
  --profile -d <device>

# 5회 반복(중앙값용)은 위 명령을 반복하며 build/integration_response_data.json 을
# perf/runs/run_N.json 으로 각각 복사해 모은다.
```

> `--no-dds` 필수: 없으면 `watchPerformance`가 VM Service 타임라인에 붙지 못하고
> `SocketException: Connection refused` 로 실패한다.

- 시나리오(`integration_test/perf_test.dart`): 600배치 pump(60Hz×10초) + 30배치마다
  1회 스크롤 + 각 배치 후 한 프레임 렌더. baseline=`NaiveWatchlistScreen`,
  after=`WatchlistScreen`, 둘 다 feed 주입 + `autoStart:false`로 pump 구동.

### 왜 `pump()` 인가 — 벽시계 측정은 **느린 후보에게 유리**하다

seed 재현성만이 이유가 아니다. 더 강한 이유는 **피드가 UI와 같은 이벤트 루프를 공유**한다는
사실에서 나온다.

`MarketFeed.start()`는 `Timer.periodic(16666μs)`이고, 이 콜백은 UI 아이솔레이트의 같은
이벤트 큐에 들어간다. 즉 피드는 "외부 서버"가 아니라 **프레임 작업과 순서를 다투는 이웃**이다.
Dart `Timer.periodic`은 밀린 주기를 몰아 실행하지 않고 **드롭**하므로:

> 프레임이 34ms 걸리면 그 구간의 타이머 콜백이 스킵된다.
> → **jank가 심할수록 실제 tick 유입량이 줄어든다. 부하가 스스로 완화된다.**

벽시계(`start()`) 기준으로 재면 baseline은 **자기가 만든 jank 덕분에 입력을 덜 받는 이득**을
본다. 느린 후보일수록 입력이 가벼워지는, 방향이 명확한 편향이다.

`pump()`는 타이머를 우회해 두 후보에 **동일한 600배치를 강제로 밀어넣는다.** 부하 자기조절이
사라지므로 "같은 일을 시켰을 때 누가 빠른가"를 비교할 수 있다. 정리하면 `pump()`가 필요한
이유는 두 겹이다.

| 층 | 제거하는 것 |
|---|---|
| seed 결정론 | tick **내용**의 run-to-run 차이 |
| 타이머 우회 | tick **양**이 후보 성능에 따라 달라지는 편향 |

**단, `pump()`는 시간을 흘리지 않는다.** 600배치가 동기 루프로 밀려 microtask로 연속 배달되므로
벽시계는 거의 흐르지 않는다. 따라서 `pump()`는 **흡수·집계·rebuild의 CPU 비용 비교**에는
정확하지만, 시간 기반 throttle(예: `Timer(100ms)`)을 쓰는 설계의 rebuild 횟수를 재면
비현실적으로 좋게 나온다. 이 프로젝트가 coalescing을 벽시계 타이머가 아니라 **프레임 경계**에
건 것이 이 함정을 피한다 — pump 경로에서도 프레임은 실제로 렌더되므로 flush 횟수가 왜곡되지
않는다. (시나리오가 "각 배치 후 한 프레임 렌더"인 이유.)

---

## 대상

**Before (baseline)** — `lib/baseline/naive_watchlist_screen.dart`
- tick 배치마다 `setState()` 로 화면 전체 rebuild
- 요약(시총)을 매 build마다 전체 2,000 순회 재계산
- `RepaintBoundary` 없음 / 정합성 처리 없음
- 기능 없음: Top-20·검색·에러 UI·정지 표시 전부 미구현

**After (개선본)** — `lib/state/market_store.dart` + `lib/ui/watchlist_screen.dart`
- 프레임당 1회 coalescing flush + 종목별 rebuild 신호 + `RepaintBoundary`
- 증분 시총 집계, Top-20 라이브 랭킹 (`Float64List` + 20슬롯 선형 선택 — §5에서
  `SplayTreeSet`에서 교체했다. §결과에 교체 전/후 두 회차가 모두 있다.)
- 정합성(역순 tick 가드)·거래정지 표시·스트림 에러 배너 **추가 구현**
- 꼬리 최적화: notifier는 "다시 그려라" 신호만 나르고, 데이터는 행이 그릴 때
  `viewOf()`로 라이브 상태에서 읽음 → 보이는 행만 뷰 객체 생성(신선도 유지)

---

## 결과 (5회 중앙값)

측정 회차가 둘 있다. 랭킹 자료구조를 교체(§5)하면서 다시 쟀기 때문이다.

| 회차 | 날짜 | 원본 | after의 Top-20 랭킹 |
|---|---|---|---|
| **1차** | 2026-07-28 | `perf/runs/run_1..5.json` | `SplayTreeSet` |
| **2차** | 2026-08-06 | `perf/runs/topk_run_1..5.json` | 선형 선택 (`_topK`) |

> **회차끼리 직접 비교하지 말 것.** 실기기는 발열·백그라운드 상태가 날마다 달라서
> **baseline(naive)조차 1차 10.74ms → 2차 11.41ms로 6% 느려졌다.** 그래서 각 회차는
> **같은 세션 안의 baseline↔after 비교**로 읽어야 한다. 그 안에서는 기기 드리프트가
> 두 시나리오에 공통으로 걸려 상당 부분 상쇄된다.
> 표 생성: `python3 perf/summarize.py --within <prefix>`

### 2차 (현재 코드) — baseline vs after

| 지표 | Before | After | 변화 |
|---|---|---|---|
| 총 프레임 수 | 639 | 636 | — |
| **평균 프레임 빌드 시간** | 11.41 ms | **8.42 ms** | **−26.2%** ✅ |
| 90th %ile 빌드 시간 | 15.85 ms | **13.46 ms** | **−15.1%** ✅ |
| **예산 초과 프레임 수** | **60** | **31** | **−48.3%** ✅ |
| young-gen GC | 156 | **72** | **−53.8%** ✅ |
| old-gen GC | 14 | **14** | **±0%** = |
| 최악 래스터 시간 | 19.09 ms | 13.23 ms | −30.7% ✅ |
| 99th %ile 빌드 시간 | 24.09 ms | 29.39 ms | +22.0% ❌ |
| 최악 프레임 빌드 시간 | 30.26 ms | 35.43 ms | +17.1% ❌ |
| 평균 래스터 시간 | 4.97 ms | 5.90 ms | +18.9% ❌ |

### 1차 (교체 전) — baseline vs after

| 지표 | Before | After | 변화 |
|---|---|---|---|
| 총 프레임 수 | 637 | 636 | — |
| **평균 프레임 빌드 시간** | 10.74 ms | **8.87 ms** | **−17.4%** ✅ |
| 90th %ile 빌드 시간 | 15.44 ms | **13.80 ms** | **−10.7%** ✅ |
| **예산 초과 프레임 수** | **52** | **29** | **−44.2%** ✅ |
| young-gen GC | 164 | **122** | **−25.6%** ✅ |
| 99th %ile 빌드 시간 | 22.89 ms | 28.91 ms | +26.3% ❌ |
| 최악 프레임 빌드 시간 | 26.55 ms | 34.23 ms | +28.9% ❌ |
| 평균 래스터 시간 | 4.68 ms | 5.54 ms | +18.4% ❌ |
| 최악 래스터 시간 | 9.93 ms | 12.31 ms | +24.0% ❌ |
| old-gen GC | 14 | 24 | +71.4% ❌ |

**두 회차의 차이가 곧 랭킹 교체의 효과**다 → **§5.5**에서 해석한다. 요약하면
old-gen GC 회귀(+71.4% → ±0%)가 사라졌고, 꼬리(99th/worst)는 여전히 baseline보다
나쁘다 — 즉 꼬리의 원인은 랭킹 계산이 아니었다.

### 회차별 분포 (노이즈 확인)

| 지표 | Before 5회 | After 5회 |
|---|---|---|
| **1차** 예산 초과 | 52, 45, 57, 44, 52 | 29, 29, 29, 29, 30 |
| **1차** 평균 빌드(ms) | 10.9, 10.5, 10.8, 10.7, 10.7 | 8.3, 8.9, 8.9, 8.9, 8.4 |
| **1차** 최악 빌드(ms) | 35.1, 28.7, 26.0, 24.8, 26.6 | 31.6, 35.6, 34.2, 34.1, 35.7 |
| **2차** 예산 초과 | — | 32, 31, 28, 31, 32 |
| **2차** 평균 빌드(ms) | — | 8.42, 8.38, 8.42, 8.64, 8.44 |
| **2차** 최악 빌드(ms) | — | 32.7, 31.9, 35.9, 35.4, 36.3 |

→ **after는 값 자체도 낮지만 편차가 거의 없다**(예산 초과 29±0.5 / 31±1.5 vs
baseline 50±6). 꼬리(worst ~32~36ms)도 두 회차 10 run 내내 안정적으로 높으므로
**노이즈가 아니라 실재하는 특성**이다.

---

## 해석

### 1) after가 이긴 곳 — 평균·중앙값·끊김 총량·할당
평균 빌드 −17%, 90th −11%, **예산 초과 프레임 −44%**, young-GC −26%.
게다가 baseline엔 없던 기능(Top-20·정합성·정지 표시·에러 UI)을 **추가하고도** 얻은 결과다.
원인:
- **coalescing** — 초당 60배치를 프레임당 1회 flush로 병합해 rebuild 횟수를 줄임.
- **rebuild 범위 축소** — 종목별 신호 + `RepaintBoundary`로 (보이는 ∩ 바뀐) 행만 갱신.
- **증분 집계** — 매 build 2,000 순회 제거(baseline 빌드 시간을 상수로 끌어올리던 주범).

### 2) after가 진 곳 — 극단 꼬리(99th/worst)와 래스터
- **래스터 +18~24%**: Top-20 스트립·정지 배지·에러 배너를 더 그리는 **기능 비용**.
  (단 래스터는 예산 16.67ms를 한 번도 안 넘음 → 병목 아님)
- **99th/worst 빌드 +26~29%**: **coalescing 버스트**. 큰 배치가 온 프레임에서 `_flush()`가
  pct 바뀐 종목마다 SplayTree `remove`+`add` + `_computeTop20()` + Top-20 카드 rebuild를
  몰아서 처리 → 그 소수 프레임만 ~34ms로 튄다.
  baseline은 매 프레임 "2,000 순회"라는 일정한 무게라 꼬리가 오히려 평탄하다.
  > ⚠️ 이 문장의 "SplayTree 갱신"에 무게를 둔 부분은 **측정으로 반박됐다**. 트리 갱신은
  > 175us(x86 AOT)에 불과했고, 실제로 제거해도 꼬리는 그대로였다 → **§5.5 ②**.
  > 또 "최대 250건"도 공칭값이고 실제로 등락률이 바뀌는 종목은 평균 100개다(§5.3).
  > 꼬리의 주범은 랭킹 계산이 아니라 **Top-20 카드 rebuild + 행 rebuild**다.

### 3) 꼬리 악화는 국지적 버그가 아니라 **coalescing의 구조적 성질**이다

2)의 "Top-20 카드 rebuild가 몰려서"는 국지적 설명이다. 한 단계 위에서 보면 일반 법칙이 있다.

> **단일 아이솔레이트에서 미룬 일은 사라지지 않는다. 어느 한 프레임이 전부 떠안는다.**

Coalescing은 작업을 *제거*하는 장치가 아니라 *합치는* 장치다. 중복 종목을 접어 총량은 줄지만,
남은 작업이 **더 적은 프레임에 몰리므로 프레임당 작업량의 분산은 반드시 커진다.** 그래서

- 평균 −17.4% / 예산 초과 −44.2% (합쳐서 총량이 줄었다)
- 99th +26.3% / worst +28.9% (몰려서 분산이 커졌다)

는 모순된 결과가 아니라 **같은 원인의 양면**이다. 다른 스레드로 넘길 수 없는 작업(build/layout)
을 프레임 단위로 묶는 설계라면 이 트레이드오프는 구조적으로 따라온다.

이 해석은 아래 "효과 없던 시도"가 왜 실패했는지도 설명한다. 그 시도는 **프레임당 할당량**을
줄이는 방향이었고, 꼬리의 원인은 할당이 아니라 **프레임당 작업량에 상한이 없는 것**이었다.
할당을 줄여도 몰림 자체는 그대로다.

### 4) 트레이드오프를 어디에 그었나
- baseline: 예산 초과 52프레임이 17~27ms에 퍼짐 → **상시 저강도 버벅임**.
- after: 예산 초과 29프레임, 대신 소수가 ~34ms로 튐 → **대체로 매끄럽고 가끔 큰 히치**.
- 60fps 체감에선 **끊김 프레임 총량이 절반**인 after가 유리하다고 판단해, "평균·끊김 횟수·
  신선도"를 취하고 "극단 꼬리 소폭 악화"를 감수했다. 신선도 200ms 제약도 프레임당 flush
  (≈12프레임 예산 내 1회)로 지켜진다.
- **§5 이후에도 이 트레이드오프의 모양은 그대로다.** 2차 측정에서도 예산 초과는
  −48.3%로 좋아졌지만 꼬리는 +17~22%로 남았다. 랭킹 교체가 바꾼 것은 꼬리가 아니라
  **평균과 할당(GC)** 이다(§5.5).

---

## 검증했지만 효과 없던 시도 (정직한 기록)

**가설:** 꼬리(worst/99th)의 주범은 "안 보이는 행까지 `SymbolView`를 매 flush 할당"하는 것.
→ **flush에서 보이는(리스너 있는) 행만 뷰를 만들도록 변경.**

**결과:** 꼬리는 **거의 움직이지 않았다**(worst 33→32ms, 노이즈 수준). 가설 기각.
작은 객체 240개 할당은 Dart young-gen에서 저렴했고, 진짜 꼬리 비용은 **뷰 할당이 아니라
랭킹 버스트**(SplayTree 갱신 + Top-20 재계산, 보이든 안 보이든 실행)에 있었다.
→ 이 변경은 성능이 아니라 **구조(신호/데이터 분리)와 신선도 보장** 이점으로 유지한다.

**다음에 꼬리를 더 줄이려면** — 3)의 법칙에서 도출되는 방향은 "작업을 더 싸게"가 아니라
**"프레임당 작업량에 상한을 두고 초과분을 이연"** 이다.

- `_flush()`에 작업 예산을 둔다: dirty가 임계치를 넘으면 행 신호까지만 처리하고
  트리 갱신·Top-20 재계산은 **다음 프레임으로 넘긴다**(시간 분할).
- 신선도 200ms ≈ **12프레임** 예산이므로 1~2프레임 이연은 요구사항을 위반하지 않는다.
  즉 이연할 여유가 스펙상 존재한다는 점이 이 방향의 근거다.
- 대안: Top-20을 매번 20개 새로 만들지 않고 이전 결과와 diff해 바뀐 카드만 교체.
  위 표의 **old-gen GC +71%**(프레임을 넘어 살아남는 객체의 promotion으로 추정)도
  함께 줄어들 가능성이 있어, 두 가설을 한 번에 검증할 수 있다.

셋 다 **측정으로 검증하지 않은 가설**이다. 이전 시도를 기각한 것과 같은 기준으로 재야 한다.

> **후속(§5에서 측정 완료).** 위 세 가설의 상태가 갈렸다.
>
> - **"랭킹 버스트가 꼬리의 주범"** → **기각.** 랭킹 계산을 36배 줄였는데도 꼬리는
>   그대로였다(§5.5 ②). 이 문단이 "진짜 꼬리 비용"이라 단정한 부분은 틀렸다.
> - **old-gen GC +71%의 promotion 추정** → **적중.** 다만 원인은 Top-20 카드가 아니라
>   **랭킹 자료구조의 할당**(박싱된 double + splay 노드)이었고, 그걸 없애자 ±0%로
>   복귀했다(§5.5 ①). 즉 이 항목은 카드 diff를 구현하기 **전에** 이미 해결됐다.
> - **작업 예산·이연(시간 분할)** → 여전히 미검증. 단 이연할 대상이던 "트리 갱신"이
>   4.8us로 사라졌으므로, 이연해서 얻을 것이 거의 없어졌다. 우선순위가 낮아졌다.
> - **카드 diff** → 이제 꼬리에 대한 **유일한 1순위 가설**이다.

---

## 5) 랭킹 자료구조 교체 — `SplayTreeSet` → 선형 선택

§해석 2)·3)에서 꼬리의 원인으로 지목한 "랭킹 버스트"를 **분리해서 측정**했다.
지목만 하고 재지 않으면 위의 "효과 없던 시도"와 같은 실수를 반복한다.

### 5.1 무엇을 왜 바꿨나

**Before** — 등락률 내림차순으로 전 종목을 `SplayTreeSet<String>`에 정렬 유지하고,
pct가 바뀐 종목만 `remove` → 값 갱신 → `add` 했다. 상위 20은 트리 앞에서 20개.

**After** (`market_store.dart` `_topK`) — 순서를 유지하는 자료구조를 없앴다.

| | Before | After |
|---|---|---|
| 상태 | `Map<String,double> _treePct` + `SplayTreeSet<String>` | `Float64List _pct` (인덱스 기반) |
| 갱신 | `remove`+`add`, O(dirty·log n) | 배열 한 칸 쓰기, **O(dirty)** |
| 선택 | 정렬 상태에서 앞 20개, O(20) | 전체 1회 스캔 + 20슬롯 삽입, **O(n)** |
| 비교자 | 외부 가변 상태(`_treePct`)에 의존 | **없음** |

핵심은 `symbols`가 스냅샷 이후 불변이라 **"인덱스 i ↔ symbols[i].code"가 안정적**이라는
점이다. 이 축이 있으면 등락률을 문자열 키 맵이 아니라 연속 메모리 배열에 담을 수 있다.

선택은 정렬이 아니라 **k슬롯 삽입 선택**이다. `floor`(현재 20위의 pct)보다 낮은 후보는
비교 1회로 탈락하므로, 2,000개 중 대부분이 분기 하나로 끝난다.

### 5.2 측정 방법

```bash
dart compile exe perf/top20_bench.dart -o /tmp/bench && /tmp/bench   # AOT 권장
dart run perf/top20_bench.dart                                        # JIT
```

- 실제 `MarketFeed`(기본 seed=20260703)에서 **600배치**를 받아, 프레임별
  "(등락률이 바뀐 종목, 새 등락률)" 목록을 한 번만 만든다.
- **동일한 입력**을 두 전략에 흘려 **랭킹 갱신 + 상위 20 선택** 비용만 잰다.
  가격 갱신·dirty Set 구성 같은 공통 비용은 제외했다(비교 대상이 자료구조이므로,
  공통항을 넣으면 차이가 희석될 뿐이다).
- 환경: **linux-x64, AOT 컴파일**. 실기기 프레임 타임(§결과)과는 다른 측정이다 —
  §5.4의 한계를 함께 읽어야 한다.
- 3회 반복해 값이 안정적임을 확인했다(avg 175.3 / 177.4 / 175.3 us).

### 5.3 결과

**입력 분포부터** — 여기가 이 분석에서 가장 중요한 수정이다.

```
dirty(등락률이 바뀐 종목) 분포: avg 100.5  p50 100  p90 182  p99 202  max 214
```

피드의 공칭값은 배치당 250건(`updatesPerBatch`)이지만, **실제로 등락률이 바뀌는 종목은
평균 100개**다. 같은 종목이 한 배치에 여러 번 오고(dirty는 Set이라 접힘), 호가 단위
반올림 때문에 tick이 와도 가격이 그대로인 경우가 있다. **공칭 부하로 추론하면 틀린다.**

| 지표 | A) SplayTreeSet | B) 선형 스캔 | 배수 |
|---|---|---|---|
| **평균** | **175.3 us** | **4.8 us** | **36.5x** |
| p50 | 174 | 5 | 34.8x |
| p90 | 314 | 6 | 52.3x |
| p99 | 361 | 7 | 51.6x |
| worst | 394 | 12 | 32.8x |
| 프레임당 비교자 호출 | 4,477 | **0** | — |

**등가성**: 600프레임 전부 Top-20이 코드·순서까지 동일(벤치가 매 프레임 대조).
회귀 테스트는 `test/top_movers_test.dart`가 무차별 전체 정렬과 대조해 상시 검증한다.

#### 왜 이겼나 — 빅오가 아니라 **상수**다

여기서 직관에 반하는 결과가 나온다. 트리가 유리한 조건은

```
dirty · log₂n < n   →   dirty < 2000 / 11 ≈ 182
```

이고 실측 dirty는 평균 100, **교차점을 넘는 프레임은 600 중 60개(10%)뿐**이다. 즉
**대부분의 프레임에서 트리는 빅오상 유리한 쪽이었는데도 36배 졌다.** 원인은 전부 상수다.

- 프레임당 비교자 호출 **4,477회**, 그 한 번마다 `_treePct` 해시 조회 2회 +
  박싱된 `double` 비교 + 동률 시 문자열 비교.
- 트리 노드 순회는 **포인터 추적** = 캐시 미스. 선형 스캔은 `Float64List`
  순차 접근이라 캐시라인을 그대로 태운다.
- `remove`/`add`는 splay 회전으로 노드 링크를 재배치한다 — 비교 횟수보다 실제 일이 많다.

> **교훈:** n이 2,000처럼 작고 데이터가 연속 메모리에 들어가면, **log n을 아끼는 것보다
> 상수를 아끼는 게 압도적으로 크다.** "O(dirty·log n)이 O(n)보다 낫다"는 판단은
> n과 상수를 대입해 확인하지 않으면 근거가 없다.

#### 검토했지만 채택하지 않은 대안 — 임계값 게이팅

"20위 경계값보다 낮은 dirty 종목은 아예 무시하고, 후보 버퍼(64)가 소진될 때만 재스캔"
하는 증분 방식도 재봤다. 평균은 더 좋았지만(p50 2us) **재스캔이 걸리는 프레임에서
worst 269us로 튀었다.** 프레임 타임 최적화의 목표는 avg가 아니라 worst/p99이므로,
avg를 조금 얻고 꼬리를 악화시키는 교환은 방향이 반대다. **매 프레임 무조건 전체를 훑는
쪽이 더 빠르고 무엇보다 예측 가능**하다 → 단순한 쪽을 택했다.

### 5.4 이 측정의 한계 — 꼬리 규명은 절반만 맞았다 (예측)

> 이 절은 §5.3(마이크로 벤치)만 손에 있던 시점의 **예측**이다. 실기기 재측정으로
> 검증한 결과는 §5.5다. 예측을 남겨두는 이유는, 맞았는지 틀렸는지 대조할 수 있어야
> 다음 가설을 세울 근거가 되기 때문이다.

§해석 2)와 "효과 없던 시도"는 34ms 꼬리의 주범을 **랭킹 버스트**로 지목했다. 이번
분리 측정은 그 진단을 **부분적으로 기각**한다.

랭킹 계산은 x86 AOT에서 **175us**다. 실기기(arm64 profile)가 3~5배 느리다고 잡아도
**0.5~0.9ms** 수준이다. 즉 **랭킹 계산만으로는 34ms 꼬리를 설명할 수 없다.**

이전 문서가 "랭킹 버스트"라 부른 것은 실은 두 가지가 섞인 이름이었다.

| 구성 요소 | 이번 변경으로 | 남은 비용 |
|---|---|---|
| 랭킹 **계산** (트리 갱신 + 상위 20 선택) | 175us → 4.8us로 제거 | 거의 없음 |
| Top-20 **카드 rebuild** (20개 위젯 재빌드 + 리스트 재생성) | 그대로 | **여기가 남았다** |

따라서 꼬리의 잔여 원인은 **랭킹 계산이 아니라 위젯 작업**일 가능성이 크다.
§"다음에 꼬리를 더 줄이려면"의 세 번째 항목("이전 결과와 diff해 바뀐 카드만 교체")이
이제 **1순위 가설**이다 — 계산을 지웠는데도 꼬리가 남으면 그것이 확증이 된다.

### 5.5 실기기 검증 (2026-08-06, EF550R 5회) — 예측이 맞았다

§5.4의 예측을 실기기에서 확인했다. 원본은 `perf/runs/topk_run_1..5.json`.

```bash
python3 perf/summarize.py                       # 회차 간 (드리프트 주의)
python3 perf/summarize.py --within topk_run_    # 회차 내 (권장)
```

**같은 세션 안의 baseline↔after 개선폭이 어떻게 달라졌나** — 회차끼리 직접 비교하면
기기 드리프트가 섞이므로, 두 회차의 "baseline 대비 개선폭"을 비교한다.

| 지표 (baseline 대비 after) | 1차 = 교체 전 | 2차 = 교체 후 | 판정 |
|---|---|---|---|
| **old-gen GC** | **+71.4%** ❌ | **±0%** = | **회귀가 사라졌다** |
| **young-gen GC** | −25.6% ✅ | **−53.8%** ✅ | 개선폭 2배 |
| 평균 빌드 시간 | −17.4% ✅ | **−26.2%** ✅ | 개선 |
| 예산 초과 프레임 | −44.2% ✅ | **−48.3%** ✅ | 소폭 개선 |
| 99th %ile 빌드 | +26.3% ❌ | +22.0% ❌ | **거의 그대로** |
| 최악 빌드 시간 | +28.9% ❌ | +17.1% ❌ | **거의 그대로** |
| 평균 래스터 | +18.4% ❌ | +18.9% ❌ | 무변화(대조군) |

#### ① 확정된 성과 — 할당 제거로 GC 회귀가 사라졌다

1차 표에서 유일하게 설명이 붙지 않던 ❌가 **old-gen GC +71%**였다. 그것이 **±0%로
정확히 baseline 수준(14회)에 복귀**했고, young-gen GC 개선폭은 −25.6% → −53.8%로 두 배가 됐다.

원인은 명확하다. 교체 전에는 프레임당 **두 종류의 힙 할당**이 있었다.

- `_treePct[c] = pct` — `Map<String,double>`에 넣는 `double`은 **박싱**된다(힙 객체).
- `SplayTreeSet.add` — 노드 객체를 새로 만든다. `remove`+`add` 쌍이니 매번이다.

dirty 평균 100 × 2종 × 636프레임 ≈ **12만 개의 단명 객체**. 이 중 일부가 old-gen으로
승격(promotion)되어 old-gen GC를 밀어올리고 있었다. 교체 후 `_pct[i] = pct`는
`Float64List`에 **언박싱 저장 = 할당 0**이다.

> 1차 문서는 old-gen +71%를 "프레임을 넘어 살아남는 객체의 promotion으로 **추정**"이라고만
> 적었다. 그 추정이 맞았고, 원인이 **랭킹 자료구조의 할당**이었음이 확인됐다.

#### ② 확정된 기각 — 꼬리는 랭킹 계산이 아니었다

99th는 +26.3% → +22.0%, worst는 +28.9% → +17.1%. **랭킹 계산을 36배 줄였는데도 꼬리는
여전히 baseline보다 20% 가까이 나쁘다.** §5.4의 예측대로다.

숫자도 앞뒤가 맞는다. 랭킹 계산은 x86에서 175us였고 기기가 5배 느려도 ~0.9ms인데,
꼬리는 30ms대다 — 애초에 설명할 수 있는 규모가 아니었다. 따라서:

> **§해석 2)·3)이 "랭킹 버스트"라 부른 꼬리의 주범은 랭킹 *계산*이 아니라
> Top-20 카드 *rebuild*와 행 rebuild, 즉 위젯 작업이다.**

`Top-20 카드 diff`(이전 결과와 비교해 바뀐 카드만 교체)가 이제 **유일하게 남은 1순위
가설**이다. 계산 쪽을 완전히 지운 상태이므로, 다음 측정은 위젯 작업만을 겨눈 깨끗한 실험이 된다.

#### ③ 대조군 검증 — 이 결론을 믿어도 되는 근거

- **평균 래스터 +18.4% → +18.9%.** 래스터는 store 코드와 무관하고(Top-20 스트립·배지를
  더 그리는 기능 비용) 실제로 **거의 움직이지 않았다.** 랭킹 교체가 건드리지 않아야 할
  지표가 안 움직였다는 것은 측정이 엉뚱한 것을 재고 있지 않다는 확인이다.
- **old-gen GC 결론은 두 가지 비교 방식에서 모두 성립한다.** 회차 내(+71.4% → ±0%)와
  회차 간(after끼리 24 → 14) 양쪽에서 같은 결론이 나오므로 드리프트 산물이 아니다.
- **기기 드리프트는 실재했다.** baseline이 10.74 → 11.41ms, 예산 초과 52 → 60으로
  느려졌다. 그래서 평균 빌드 −17.4% → −26.2% 같은 **개선폭 수치는 절대값으로 받지 말고
  방향만 읽어야 한다**(드리프트가 baseline을 부풀리면 개선폭이 커 보인다).

**남은 공백:** 위 ②의 결론은 "랭킹 계산을 지워도 꼬리가 남는다"는 **소거법**에 근거한다.
카드 rebuild가 꼬리의 주범이라는 **직접 증거는 아직 없다.** 그건 카드 diff를 구현해
꼬리가 실제로 내려가는지 재야 확정된다.
