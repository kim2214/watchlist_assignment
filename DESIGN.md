# DESIGN.md — 아키텍처 결정과 근거

실시간 관심종목(2,000종목, 초당 최대 15,000 갱신) 화면의 설계 문서.
성능 수치와 측정 방법은 `PERF.md`를 참조한다. 이 문서는 **왜 이 구조를 택했고
무엇을 왜 버렸는지**에 집중한다.

---

## 1. 목표와 제약

- 2,000행을 60fps로 스크롤(프레임 예산 16.67ms).
- **신선도**: 보이는 행 가격은 최신 tick 후 200ms 이상 stale 금지.
- feed는 깨끗하지 않다: 지연·역순 tick, 거래정지, 스트림 에러.
- **정합성과 성능을 동시에** 지키는 게 핵심.

두 요구는 부분적으로 상충한다(coalescing을 세게 걸면 프레임은 안정되나 신선도 악화).
어디에 선을 긋는지가 §7.

---

## 2. 계층 구조와 경계

```
Presentation (Widgets)   WatchlistScreen · DetailScreen
   ▲ 불변 뷰(SymbolView/DetailView) + rebuild 신호(int)
State / Application       MarketStore
   ▲ 정규화된 도메인 상태(정합성 처리 완료)
Domain 경계 = MarketStore._onBatch (raw→도메인 변환·정합성 흡수)
   ▲ raw
Data Source (수정 금지)   MarketFeed (seed)
```

**핵심 원칙: raw `QuoteTick`은 `MarketStore` 밖으로 나가지 않는다.**
프레젠테이션은 계산이 끝난 불변 객체(`SymbolView`, `DetailView`, `MarketSummary`,
`TopMover`)만 본다. 등락률·시총·시고저 같은 파생값, 그리고 역순 tick·정지·에러 같은
"지저분함"은 전부 Store 경계에서 흡수한다.

**경계를 Store 한 곳에 둔 이유**: feed의 비정상 특성(정합성 이슈)과 성능 장치
(coalescing·증분 집계·랭킹)가 전부 "tick을 받아 뷰 상태로 바꾸는" 동일 지점에 모인다.
이를 여러 계층에 흩으면 정합성 규칙이 중복·누락되기 쉽다. `lib/seed/`에는 어떤 처리도
넣지 않는다(데이터 소스의 책임이 아님).

---

## 3. 상태관리 — "신호와 데이터의 분리"

**선택: 순정 `ValueNotifier` 기반. 단, notifier는 데이터가 아니라 "다시 그려라" 신호
(`ValueNotifier<int>` 카운터)만 나른다. 데이터는 행이 그릴 때 `store.viewOf(code)`로
라이브 상태에서 직접 읽는다.**

- 종목마다 신호 하나(`_RowSignal extends ValueNotifier<int>`). 행 위젯은 자기 종목
  신호만 구독 + `RepaintBoundary`.
- 요약·Top-20·에러배너·필터버전은 각각 별도 notifier → 행 rebuild와 격리.

이 구조가 주는 것:
1. **rebuild 범위 최소화** — `ListView.builder`(보이는 행만 생성) ∩ 신호가 온 종목만
   → 실제 rebuild는 (보이는 ∩ 바뀐) 행뿐. 안 보이는 행은 신호조차 받지 않는다.
2. **신선도 보장** — 스크롤로 새로 보이게 된 행도 그리는 순간 `viewOf()`로 최신 상태를
   읽으므로 stale이 원천적으로 없다. (notifier에 데이터를 실었다면, 안 보이는 동안
   갱신을 건너뛴 낡은 값이 스크롤 인 시점에 표시되는 버그가 생긴다 — §6에서 기각.)

의존성(Provider/Riverpod/Bloc)을 쓰지 않은 이유는 §6.

---

## 4. 데이터 흐름 — Coalescing (시간축 최소화)

`MarketFeed`는 초당 60배치를 흘린다. 이를 즉시 UI에 반영하지 않는다.

- `_onBatch`: 상태 맵만 갱신하고 바뀐 코드를 `_dirty` 집합에 모은다.
- `_scheduleFlush`: `addPostFrameCallback` + `scheduleFrame`으로 **프레임당 1회**
  `_flush`를 예약(중복 예약 무시).
- `_flush`: dirty 종목 중 보이는 행만 신호, 증분 집계·랭킹 갱신.

효과: 초당 60배치가 와도 UI 반영은 프레임당 1회. 같은 종목이 한 프레임에 여러 번
바뀌어도 마지막 값만 그린다. 200ms(≈12프레임) 예산 안이라 여유롭다.

---

## 5. 정합성 / 엣지 모델

### 5-1. 지연·역순 tick
종목별 마지막 반영 시각 `_lastTs`를 보관하고, `tick.timestampMs > _lastTs`일 때만
가격·상태를 갱신한다. **도착 순서가 아니라 관측 시각(`timestampMs`)이 진실.**
과거 tick은 건너뛰어 **가격이 되돌아가지 않는다.**
예외: `dayVolume`은 누적값이라 시각과 무관하게 **max로만 증가**시켜 역순에도 감소를 막는다.

### 5-2. 거래정지 (halt)
- `status == halted` → 해당 종목 `_halted=true`(가격은 feed가 직전가로 고정),
  이후 `active` tick이 오면 해제. 행/상세에 회색 + "정지" 배지.
- **파생값 판단(스스로 정한 것)**:
  - 등락률·순위: 정지 종목도 **마지막 값으로 순위에 잔류**(배지로 구분). 순위에서 빼면
    정지·해제 때마다 순위가 출렁여 오히려 혼란.
  - 시총 합계: 정지 종목도 **포함**(고정가 × 상장주식수). 제외하면 합계가 튄다.
  - "표시 중 수": 화면에 보이므로 **포함**.
- 구독 시작 시점에 이미 정지 중인 종목은 첫 tick 전엔 알 수 없다 → **active로 가정**,
  첫 halted tick에 보정(seed 특성상 불가피).

### 5-3. 스트림 에러
`listen(onError:..., cancelOnError:false)`로 **구독을 끊지 않는다.** 에러 시 상단 배너로
"일시적 지연 — 복구 중"을 노출하고, 다음 성공 배치가 오면 자동으로 배너를 지운다.
회귀 테스트는 `transientErrorProbability:0.1`로 에러 후 배치 소비가 이어지는지 검증.

---

## 6. 집계와 랭킹 — 전체 순회/재정렬 없이

- **시총 합계(증분)**: `_totalMarketCap += (newPrice - old) * shares`. 배치 크기만큼만.
  (baseline은 매 build 2,000 순회였다.)
- **Top-20 라이브**: `SplayTreeSet`을 등락률 내림차순으로 유지. `_flush`에서 pct가 바뀐
  종목만 `remove`→값 갱신→`add`(O(log n)). 트리 정렬 기준값은 `_treePct`에 따로 보관해
  트리 불변식을 지킨다(변경 전 제거·변경 후 삽입). 매 tick 전체 정렬 없음.

### 필터가 걸렸을 때의 기준 (스스로 정한 것)
- **필터가 걸리면 시총 합계·Top-20도 필터 집합 기준**이다(사용자가 보는 집합과 일치).
  구현: 통과 집합만 순회/정렬(집합 크기에 비례, bounded).
- **표시 중 종목 수 = 필터 통과 수.**
- **필터 아웃 종목**: 렌더되지 않아 신호를 안 받는다(rebuild 0). 단 상태 맵은 계속
  갱신되어, 필터를 풀면 즉시 최신값으로 보인다(정합성 유지).

---

## 7. 성능 ↔ 신선도 트레이드오프 — 선을 어디에 그었나

- **프레임당 1회 coalescing**로 시간축을 눌렀다. 200ms 예산이 60Hz에서 ≈12프레임이라,
  프레임당 1회 flush로도 신선도를 넉넉히 지키면서 rebuild 폭주를 막는다. 더 세게(예:
  N프레임마다) 묶으면 프레임은 더 안정되지만 신선도 여유를 깎으므로, **"프레임당 1회"를
  상한선**으로 잡았다.
- **공간축**은 (보이는 ∩ 바뀐) 행으로 최소화 + `RepaintBoundary`로 래스터 격리.
- 측정 결과(5회 중앙값, `PERF.md`): 평균 빌드 −17%, **예산 초과 프레임 −44%(52→29)**,
  young-GC −26%. 대신 **극단 꼬리(99th/worst)는 +26~29%** — 큰 배치 프레임에서 랭킹
  버스트가 몰리는 coalescing의 트레이드오프. baseline은 "상시 저강도 버벅임(초과 52)",
  after는 "대체로 매끄럽고 가끔 큰 히치(초과 29)"다. **끊김 총량이 절반인 쪽**을 택했다.

---

## 8. 초성 검색

`codeUnitAt` 기반으로 직접 구현(라이브러리 미사용 — 로직 단순, 의존성 최소).
- 완성형 한글 배열 규칙 `(code-0xAC00)~/588`로 초성 인덱스를 얻고, **호환 자모
  (U+3131…)로 매핑**해 키보드 입력과 매칭한다(한글 자모 U+1100…과 코드포인트가 달라
  이 매핑이 없으면 안 맞는다 — 핵심 함정).
- 3-모드: 숫자 포함→코드 부분일치 / 전부 초성→초성 **앞일치(startsWith)** /
  그 외→이름 **부분일치(contains)**. 초성은 앞일치, 이름은 부분일치의 비대칭은
  과제 예시(앞에서부터)와 주식 검색 UX에 맞춘 의도된 선택.
- **성능**: 초성 인덱스는 이름 불변이라 **초기화 때 1회** 계산. keystroke는 **debounce
  200ms** 후 1회 필터. 필터 스캔(O(n))은 쿼리 변경 시에만 — tick과 무관. →
  "매 tick·keystroke 전체 재계산 금지" 충족.

---

## 9. 상세 화면과 구독 수명

- 동일 feed에서 포커스 종목 신호만 구독해 실시간 갱신(coalesced).
- **시/고/저**는 feed에 없으므로 tick 흐름에서 누적(전 종목 O(1), `timestampMs` 가드 안).
  시가는 "구독 시점가"로 정의.
- **스파크라인**: 포커스 종목만 최근 60개 링버퍼(무한 증가 방지). 매 tick이 아니라
  상세 rebuild(coalesced) 시에만 그리고, `CustomPainter.shouldRepaint`으로 최소화.
- **구독 수명 관리**: 상세 진입 시 `openDetail`로 목록 갱신을 pause(포커스 종목만 신호,
  요약·Top-20 갱신 생략 — 목록 위젯이 offstage이므로). 랭킹 트리는 계속 유지해 복귀 시
  정확. `closeDetail`에서 재개 + 보이는 행/집계를 강제 새로고침해 목록을 현재 상태로 복구.

---

## 10. 검토했지만 기각한 대안

1. **Riverpod `family` / Bloc 등 상태관리 프레임워크**
   → 2,000개 provider의 오버헤드와 "select" 튜닝, 학습된 마법이 오히려 rebuild 제어를
   흐린다. 순정 `ValueNotifier` per-symbol이 rebuild 경계를 가장 투명하게 통제하고
   의존성도 없다. **기각.**

2. **notifier에 데이터(`ValueNotifier<SymbolView>`)를 싣기** (초기 구현)
   → 안 보이는 행까지 매 flush 뷰 객체를 할당하는 낭비 + 스크롤 인 시 낡은 값 표시 위험.
   "신호(int)만 싣고 데이터는 `viewOf`로 라이브 읽기"로 전환해 할당을 보이는 행으로
   한정하고 신선도를 구조적으로 보장. (성능 효과 자체는 노이즈 수준이었으나 구조·정합성
   이점으로 채택 — `PERF.md` §검증했지만 효과 없던 시도.) **초기안 기각.**

3. **feed/집계를 별도 isolate로 분리**
   → 측정상 병목은 tick "생성"이 아니라 main의 랭킹·rebuild. feed만 isolate로 빼면
   초당 15,000 tick의 **직렬화·역직렬화 비용이 main에 새로 얹혀** 이득을 상쇄한다.
   집계까지 isolate로 옮겨 작은 diff만 반환하면 꼬리엔 도움될 수 있으나, 결정론적
   `pump()` 벤치가 깨지고 복잡도가 급증한다. 더 싸고 확실한 on-main coalescing/스로틀을
   우선. **기각(향후 재검토 여지).**

4. **Top-20를 매 tick 전체 정렬(`sort(2000)`)**
   → O(n log n)×60Hz는 과하다. pct 변경 종목만 갱신하는 `SplayTreeSet`으로 대체. **기각.**

5. **"보이는 20행만" 수동으로 골라 갱신** (초기 아이디어)
   → 뷰포트 크기·스크롤 상황에서 일부 보이는 행 누락·신선도 위반 버그. `ListView.builder`
   (보이는 것만)+종목별 신호(바뀐 것만)의 조합으로 **구조상 저절로** (보이는 ∩ 바뀐)만
   갱신되게 하는 편이 견고. **기각.**

---

## 11. 알려진 한계 / 향후

- **꼬리(worst) 프레임**: 큰 배치에서 랭킹 버스트가 몰림. Top-20 재계산·트리 갱신을
  N프레임마다 스로틀하면 완화 가능(측정 검증 필요).
- **시총 증분 합산의 부동소수 누적 오차**: 장시간이면 미세 드리프트 가능 → 주기적 전체
  재계산으로 보정 가능.
- **스파크라인 히스토리**: 상세를 연 시점부터 기록(세션 이전 구간은 없음) — 전 종목
  히스토리 보관(2,000×N)의 메모리 비용을 피한 트레이드오프.
- **시가 정의**: 진짜 장 시가가 아니라 "구독 시점가"(seed가 시가를 주지 않음).

---

## 12. 테스트 / 검증

- 엣지 회귀(`test/`): 역순 tick(가격 미역행)·거래정지 표시·스트림 에러 복구, 초성/완성형/
  코드 검색, 상세 시고저 누적·스파크라인 상한. 총 14개 통과.
- 재현 가능한 프레임 벤치(`integration_test/perf_test.dart`): `pump()` 결정론 시나리오로
  baseline/after 프레임 타임 측정(`PERF.md`).
- `flutter analyze` 클린.
